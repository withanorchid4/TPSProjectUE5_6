# 多人联机测试 — 当前瓶颈与解决方案

> 日期：2026-05-15

## 当前状态

服务端 + 客户端网络功能已全部完成，联机逻辑验证通过，但**无法流畅地进行多人测试**。

---

## 瓶颈分析

### A 方案：单机双进程（两个独立 UE 窗口）

**问题**：Windows 对后台进程降帧，后台 UE 窗口帧率降至 ~13fps，导致：
- 后台玩家（A）只发送 3-4 次移动/s
- 前台玩家（B）收到的更新频率低，远程玩家移动卡顿
- 13fps 渲染本身就卡，插值无法补帧率

**根因**：操作系统层面的 GPU 资源调度优先前台窗口，非代码问题。

**可能的缓解手段**（均无法根本解决）：
- 禁用 Efficiency Mode（任务管理器）
- 双显示器部分可见
- 客户端插值优化（只能补位置精度，补不了帧率）

**结论**：单机双窗口方案在 Windows 上无法让两边都跑满帧，不适合流畅测试。

---

### B 方案：双机双进程（两台电脑各跑一个 UE）

**问题**：客户端 `NetworkManager.DEFAULT_HOST = "127.0.0.1"`，硬编码本地地址，无法连接远程服务器。

**阻塞点**：
1. WBP_Login 没有服务器地址输入框（只有账号/密码）
2. `LoginPanel._connect_and_action()` 调 `nm.connect()` 不传 host/port
3. 服务端机器防火墙需放行 9999 端口

**解决难度**：低，改动量小。

---

## 预采用解决方案：B 方案（双机双进程）

### 需要改的文件

| 文件 | 改动 |
|------|------|
| WBP_Login 蓝图 | 新增服务器地址 EditableTextBox（`etb_server`） |
| `Content/Scripts/ui/login_ui.py` | 读取 `etb_server`，解析 host:port，传给 `nm.connect(host, port)` |
| `NetworkManager.DEFAULT_HOST` | 保持 `127.0.0.1` 作为默认值（本地测试不变） |

### 改动细节

1. **WBP_Login 蓝图**：在账号输入框上方加一个 `etb_server`（EditableTextBox），默认文本填 `127.0.0.1:9999`
2. **login_ui.py**：
   - `_connect_and_action()` 中读取 `etb_server`，解析为 host/port
   - 调用 `nm.connect(host, port)` 传入实际地址
3. **防火墙**：服务端机器 Windows 防火墙入站规则放行 TCP 9999

### 验证步骤

1. A 机器启动 server（`python main.py`）
2. A 机器 UE 登录，服务器地址填 `127.0.0.1:9999`
3. B 机器 UE 登录，服务器地址填 `A的局域网IP:9999`
4. 双方应能看到对方并流畅移动

---

## 备选：A 方案改进（如需回退）

如果实在没有第二台电脑，可考虑：
- 将 NetworkManager 从单例改为多实例，支持 "Number of Players = 2" 分屏 PIE
- 改动量中等，但有架构风险（项目接近收尾）

---

## 备注

- 服务端已绑定 `0.0.0.0:9999`，天然支持跨机器连接
- TCP + protobuf 协议无跨机器兼容问题
- A 方案本地测试（`127.0.0.1`）在 B 方案完成后仍可用
